---
type Props = {
  title: string;
  class?: string;
};

const { title, class: className = 'text-primary' } = Astro.props;
---

<h2 class="text-2xl md:text-3xl lg:text-5xl font-brand text-center" class:list={[ className ]} >
  {title}
</h2>
