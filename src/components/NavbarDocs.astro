---
import { socialLinks } from "../utils/contants";
---

<nav class="w-full h-16 bg-primary shadow">
  <div class="mx-auto max-w-6xl px-5 md:px-8 h-full flex justify-between items-center">
  <img
    src="/image/logo.webp"
    alt="Ilogs"
    class="icon w-16 h-16 md:w-20 md:h-20"
  />

  <div class="flex gap-3">
    {socialLinks.map(({ name, href, icon: Icon, iconClass }) => (
      <a
        href={href}
        target="_blank"
        rel="noopener noreferrer"
        aria-label={name}
        class={`icon fill-light ${iconClass ?? ""}`}
      >
        <Icon />
      </a>
    ))}
  </div>
  </div>
</nav>

<script>
  import { gsap } from "gsap";

  gsap.from(".icon", {
    y: -100,
    stagger: 0.25,
    duration: 0.3,
  });
</script>
